<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href= <%= root_path %> >HOME</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    

   
    <% if user_signed_in? %> 
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <a class="nav-link active" aria-current="page" href=<%=uploads_index_path%>>MY POSTS</a>
        <%= link_to "UPLOAD POST", new_upload_path %>
        </li>
        </ul>
        <ul>
          <%= button_to "Light-Mode", "#", class:"btn btn-primary action-button mx-2 my-1" %>
          <%= button_to "Dark- Mode", "#", class:"btn btn-primary action-button mx-2" %>
        </ul>
        <div class="me-2">
        Welcome :- <%= current_user.email %> 
        </div>
        <% if current_user.avatar.attached? %>
          <%= image_tag current_user.avatar, class:"user-avatar" %>
        <% end %>     

        <%= link_to 'Edit Profile ', edit_user_registration_path,class:"btn btn-primary text-center ms-3   me-3" %>

        <%= button_to "Sign out", destroy_user_session_path, class: "btn btn-primary", method: :delete %> 

    <% else %>
      <div>
        <%= button_to "Sign in", new_user_session_path, class: "btn btn-primary sign_in_button" %>
      </div> 
    <% end %>
  </div>
  </div>
</nav>
